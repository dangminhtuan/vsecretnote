{
  "name": "vcomp",
  "version": "1.0.0",
  "description": "Vietnamese Compression Protocol for AI and Data Storage",
  "main": "vcomp.js",
  "type": "module",
  "files": [
    "vcomp.js",
    "data.js",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "node tools/master_sync.mjs",
    "deploy:m": "node tools/master_sync.mjs && vite build && git add . && git commit -m \"update\" && git push && npx wrangler pages deploy dist --project-name mat-ma-thoi-gian",
    "deploy:s": "node tools/master_sync.mjs && vite build && git add . && git commit -m \"update\" && git push && npx wrangler pages deploy dist --project-name streamlai",
    "deploy:t": "node tools/master_sync.mjs && vite build && git add . && git commit -m \"update\" && git push && npx wrangler pages deploy dist --project-name townai"
  },
  "keywords": [
    "vietnamese",
    "compression",
    "ai",
    "token",
    "base60"
  ],
  "author": "TimeCypher",
  "license": "MIT",
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "opentype.js": "^2.0.0"
  }
}
